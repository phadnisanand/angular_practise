import { StudentdemoPipe } from './studentdemo.pipe';

describe('StudentdemoPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentdemoPipe();
    expect(pipe).toBeTruthy();
  });
});
